import 'package:flutter/material.dart';

class CartPaage extends StatelessWidget {
  const CartPaage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Cart Page'),
    );
  }
}